# Cibles Cargo, ou qu'elles soient (racine, spikes/*, worktrees) —
# patron GLOBAL depuis l'audit 2026-09-01 : l'enumeration par spike
# laissait des trous (spikes/kiosque-precharge/target n'y etait pas).
target/
.claude/settings.local.json
.claude/worktrees/
apps/desktop/gen/
e2e/node_modules/
apps/desktop/ui-v2/node_modules/
apps/desktop/ui-v2/dist/
e2e/test-results/
e2e/playwright-report/

# Logs: field traces of scripts/run-wind.ps1, span logs of the benches,
# maj.log — NEVER committed, wherever they are. The root-only pattern
# (`/*.log`) let 9 MB of field logs into the PUBLIC repository under
# spikes/ (audit 2026-09-01, S1-1): global pattern, plus target/ under
# any spike.
*.log
