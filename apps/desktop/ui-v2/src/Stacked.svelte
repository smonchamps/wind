<!-- The pile in miniature (D5): three offset elevations, the set-aside
     fan's visual reduced to a glyph. One drawing for the Feed's and the
     Paper trail's rank groups (Lot 5 E13e, D-47: it was hand-copied).
     Decorative: hidden from assistive technology. -->
<span class="stacked" aria-hidden="true"><span></span><span></span><span></span></span>

<style>
  .stacked { position:relative; width:20px; height:16px; flex:none; }
  /* V14: zero radius — the pile's sheets are bare rectangles, like the
     set-aside pile's visual. */
  .stacked span {
    position:absolute; inset:0; background:var(--surface);
    border:1px solid var(--border);
  }
  .stacked span:nth-child(1) { transform:translate(4px, -4px); }
  .stacked span:nth-child(2) { transform:translate(2px, -2px); }
</style>
